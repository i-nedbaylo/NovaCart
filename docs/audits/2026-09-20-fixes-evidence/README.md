# Проверки исправлений NovaCart

Итоговый отчёт: [2026-09-20-fixes.md](../2026-09-20-fixes.md).

Актуальные доказательства итогового кода:

- `build-final.log`: Release-сборка, 0 ошибок, 1 предупреждение Aspire CLI bundle.
- `final-unit-tests.log` и `final-unit-results/`: 187 unit/architecture.
- `final-integration-tests.log` и `final-integration-results/`: 6 PostgreSQL/Redis.
- `final-e2e-tests.log` и `final-e2e-results/`: один успешный полный сценарий, включая SSR-пагинацию, параллельный checkout и отмену после оплаты.
- `vulnerable-packages-verified.json`: 43 проекта, нет известных уязвимых пакетов.
- `restore-masstransit.log`: итоговое восстановление с NU1901–NU1904 как ошибками.
- `migration.log`: создание миграции Ordering; фактическое применение к тестовой БД проверено интеграционным тестом и E2E.

Остальные журналы — промежуточная диагностика: первоначальные ошибки сборки, загрузки NuGet, TLS локального DCP и отсутствие лицензии MassTransit 9. Они сохранены для проверяемости хода исправлений и не являются итоговым результатом. Последующие успешные прогоны перечислены выше. Автоматически исключённые фильтром сборки не включены в число успешных unit-тестов.
