{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_NOTE": "The JWT signing key is injected by the AppHost (env var Jwt__Secret) and never stored here. To run this service standalone, set Jwt:Secret via user-secrets or an environment variable.",
  "Jwt": {
    "Issuer": "NovaCart.Identity",
    "Audience": "NovaCart.Client",
    "ExpirationInMinutes": 60
  }
}
