{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_NOTE": "The JWT signing key is injected by the AppHost (env var Jwt__Secret) and never stored here. Issuer/Audience are public identifiers that must match the Identity service.",
  "Jwt": {
    "Issuer": "NovaCart.Identity",
    "Audience": "NovaCart.Client"
  }
}
